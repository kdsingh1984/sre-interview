package main

import (
	"sort"
	"strings"
)

func sherlockAndAnagrams(s string) int32 {
	stringArr := strings.Split(s, ",")
	for i:=0;i<len(s);i++ {
		for j:=1;i<len(s)
	}
	sort.Strings(stringArr[:1])
}
